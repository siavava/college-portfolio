/**
 * This file defines and exports injection keys used for shared data
 * that's passed around the app.
 */
// import type { InjectionKey } from "vue"

// import type { MenuOptions } from "../types"

export const MENU_OPTIONS = Symbol() as InjectionKey<MenuOptions>
